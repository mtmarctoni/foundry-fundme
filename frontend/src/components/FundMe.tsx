import { useEffect, useState } from 'react';
import { ethers } from 'ethers';

import { useContract } from '../hooks/useContract.ts';

const FundMe: React.FC = () => {

    return (
        <div>
hey
        </div>
    );
};
const contractABI: any[] = [
    // Add the ABI of your FundMe contract here
];
